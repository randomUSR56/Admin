<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Admin.ViewModels"
             xmlns:models="clr-namespace:Admin.Models"
             x:Class="Admin.Views.CarsPage"
             x:DataType="vm:CarsViewModel"
             Title="Cars">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20" RowSpacing="12">

        <!-- Search Bar -->
        <Grid Grid.Row="0"
              ColumnDefinitions="*,Auto,Auto"
              ColumnSpacing="10">
            <Entry Text="{Binding SearchText}"
                   Placeholder="Search cars..."
                   ReturnCommand="{Binding SearchCommand}"
                   ClearButtonVisibility="WhileEditing" />

            <Button Grid.Column="1"
                    Text="Search"
                    Command="{Binding SearchCommand}"
                    HeightRequest="40"
                    CornerRadius="6"
                    FontSize="13" />

            <Button Grid.Column="2"
                    Text="+ New Car"
                    Command="{Binding CreateCarCommand}"
                    HeightRequest="40"
                    CornerRadius="6"
                    FontSize="13"
                    BackgroundColor="{StaticResource Primary}" />
        </Grid>

        <!-- Error -->
        <Border Grid.Row="1"
                Stroke="{StaticResource Red100Accent}"
                BackgroundColor="#33FF0000"
                StrokeShape="RoundRectangle 8"
                Padding="12"
                IsVisible="{Binding HasError}"
                VerticalOptions="Start">
            <Label Text="{Binding ErrorMessage}"
                   TextColor="{StaticResource Red100Accent}"
                   FontSize="13" />
        </Border>

        <!-- Cars List -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Cars}"
                        IsVisible="{Binding HasError, Converter={StaticResource InvertBoolConverter}}">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="8">
                    <Label Text="No cars found"
                           FontSize="16"
                           HorizontalTextAlignment="Center"
                           TextColor="{StaticResource Gray400}" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Car">
                    <Border StrokeShape="RoundRectangle 8"
                            Padding="14"
                            Margin="0,4"
                            BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#212121}">
                        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12">

                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding DisplayName}"
                                       FontSize="15"
                                       FontAttributes="Bold" />
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding LicensePlate, StringFormat='Plate: {0}'}"
                                           FontSize="12"
                                           TextColor="{StaticResource Gray400}" />
                                    <Label Text="{Binding Color, StringFormat='Color: {0}'}"
                                           FontSize="12"
                                           TextColor="{StaticResource Gray400}"
                                           IsVisible="{Binding Color, Converter={StaticResource InvertBoolConverter}, ConverterParameter=null}" />
                                </HorizontalStackLayout>
                                <Label Text="{Binding Vin, StringFormat='VIN: {0}'}"
                                       FontSize="11"
                                       TextColor="{StaticResource Gray400}"
                                       MaxLines="1"
                                       LineBreakMode="TailTruncation" />
                            </VerticalStackLayout>

                            <Button Grid.Column="1"
                                    Text="Edit"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CarsViewModel}}, Path=ViewCarCommand}"
                                    CommandParameter="{Binding .}"
                                    HeightRequest="34"
                                    CornerRadius="6"
                                    FontSize="12"
                                    Padding="12,0"
                                    VerticalOptions="Center" />

                            <Button Grid.Column="2"
                                    Text="Delete"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CarsViewModel}}, Path=DeleteCarCommand}"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="{StaticResource Red100Accent}"
                                    HeightRequest="34"
                                    CornerRadius="6"
                                    FontSize="12"
                                    Padding="12,0"
                                    VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Loading -->
        <ActivityIndicator Grid.Row="1"
                           IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="{StaticResource Primary}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />

        <!-- Pagination -->
        <Grid Grid.Row="2"
              ColumnDefinitions="Auto,*,Auto"
              Padding="0,8,0,0">
            <Button Text="Previous"
                    Command="{Binding PreviousPageCommand}"
                    FontSize="13"
                    HeightRequest="36"
                    CornerRadius="6"
                    BackgroundColor="{StaticResource Gray600}" />

            <Label Grid.Column="1"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   FontSize="13">
                <Label.Text>
                    <MultiBinding StringFormat="Page {0} of {1} ({2} total cars)">
                        <Binding Path="CurrentPage" />
                        <Binding Path="LastPage" />
                        <Binding Path="TotalCars" />
                    </MultiBinding>
                </Label.Text>
            </Label>

            <Button Grid.Column="2"
                    Text="Next"
                    Command="{Binding NextPageCommand}"
                    FontSize="13"
                    HeightRequest="36"
                    CornerRadius="6"
                    BackgroundColor="{StaticResource Gray600}" />
        </Grid>
    </Grid>
</ContentPage>
