<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Admin.ViewModels"
             x:Class="Admin.Views.UserDetailPage"
             x:DataType="vm:UserDetailViewModel"
             Title="{Binding PageTitle}">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="True" IsEnabled="True" TextOverride="Back" />
    </Shell.BackButtonBehavior>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="← Back" Clicked="OnBackClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView Padding="20">
        <VerticalStackLayout Spacing="16"
                             MaximumWidthRequest="500"
                             HorizontalOptions="Center">

            <!-- Error -->
            <Border Stroke="{StaticResource Red100Accent}"
                    BackgroundColor="#33FF0000"
                    StrokeShape="RoundRectangle 8"
                    Padding="12"
                    IsVisible="{Binding HasError}">
                <Label Text="{Binding ErrorMessage}"
                       TextColor="{StaticResource Red100Accent}"
                       FontSize="13" />
            </Border>

            <!-- Name -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Name" FontSize="13" TextColor="{StaticResource Gray300}" />
                <Entry Text="{Binding Name}"
                       Placeholder="Full name" />
            </VerticalStackLayout>

            <!-- Email -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Email" FontSize="13" TextColor="{StaticResource Gray300}" />
                <Entry Text="{Binding Email}"
                       Placeholder="user@example.com"
                       Keyboard="Email" />
            </VerticalStackLayout>

            <!-- Password -->
            <VerticalStackLayout Spacing="6">
                <Label FontSize="13" TextColor="{StaticResource Gray300}">
                    <Label.Text>
                        <Binding Path="IsExistingUser"
                                 Converter="{StaticResource BusyTextConverter}"
                                 ConverterParameter="Password (leave blank to keep current)|Password" />
                    </Label.Text>
                </Label>
                <Entry Text="{Binding Password}"
                       Placeholder="Enter password"
                       IsPassword="True" />
            </VerticalStackLayout>

            <!-- Role -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Role" FontSize="13" TextColor="{StaticResource Gray300}" />
                <Picker ItemsSource="{Binding AvailableRoles}"
                        SelectedItem="{Binding SelectedRole}"
                        Title="Select role" />
            </VerticalStackLayout>

            <!-- Actions -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,16,0,0">
                <Button Text="Cancel"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="{StaticResource Gray600}"
                        CornerRadius="8"
                        HeightRequest="44" />

                <Button Grid.Column="1"
                        Text="{Binding IsBusy, Converter={StaticResource BusyTextConverter}, ConverterParameter='Save|Saving...'}"
                        Command="{Binding SaveCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                        CornerRadius="8"
                        HeightRequest="44" />
            </Grid>

            <!-- Loading -->
            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
